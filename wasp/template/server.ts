import { Exploit${CVE}Request, Exploit${CVE}Response } from '../../shared/vulnerability/${CVE}';

export const exploit${CVE} = async (
  args: Exploit${CVE}Request,
  context: any,
): Promise<Exploit${CVE}Response> => {
  return {}
}